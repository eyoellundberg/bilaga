import { Paperclip, ArrowUpRight, Terminal } from 'lucide-react';
export const metadata={title:'Connect your agent — Bilaga'};
const endpoints=[['GET /api/config','See upload limits and availability.'],['GET /api/quote?bytes=…','Get the future transfer price. No charge in this preview.'],['POST /api/transfers','Create an upload with a filename and size in bytes.'],['PUT /api/transfers/{id}/parts/{n}','Upload one binary chunk. Retry failed chunks.'],['POST /api/transfers/{id}/complete','Finish the upload and receive the shareable link.'],['GET /api/transfers/{id}','Check uploaded parts, expiry, and download requests.'],['POST /api/transfers/{id}/sent','Record that your agent delivered the link.'],['DELETE /api/transfers/{id}','Revoke the link and delete the stored file.']];
export default function Docs(){return <main className="shell"><header className="site-header"><a className="brand" href="/"><Paperclip size={27}/>bilaga<span className="brand-dot">.</span></a><nav><a href="/">Send a file <ArrowUpRight size={15}/></a></nav></header><article className="docs-content"><p className="eyebrow">FOR YOUR AGENT</p><h1>One connection.<br/>A simpler handoff.</h1><p>Give your agent this website’s URL and a test token. It can read <a href="/llms.txt">llms.txt</a>, upload your file, and return a link you can share anywhere.</p><p className="notice">Private preview · up to 1 GB per file · no charges. Test tokens come from the owner. Google sign-in and prepaid billing are coming next.</p><h2>Start with a file</h2><p>Use the <a href="/bilaga.py" download>Python client</a> with Python 3.10 or later. It uses the standard library and retries failed chunks. Inspect the downloaded script before running it. Set <code>BILAGA_TOKEN</code> in your environment, then replace the origin and filename below.</p><pre>{`python3 bilaga.py \\\n  --base https://YOUR-BILAGA-ORIGIN \\\n  --file report.pdf`}</pre><p>The response includes the shareable link and expiry. Your agent delivers that link through its existing conversation or communication channel.</p><h2>Check the handoff</h2><pre>{`python3 bilaga.py --base https://YOUR-BILAGA-ORIGIN \\\n  --status TRANSFER_ID`}</pre><p>Poll at most once every 15 seconds. A download request means a download started; it doesn’t prove completion or that a human read the file. Bilaga only marks a transfer “sent” when the agent reports delivery.</p><h2>A small API, end to end</h2><p>Send your token as <code>Authorization: Bearer …</code>. Keep it private: it can manage the transfers created with it. Recipient links never contain the token.</p><div style={{overflowX:'auto'}}><table><thead><tr><th>Endpoint</th><th>What it does</th></tr></thead><tbody>{endpoints.map(([endpoint,detail])=><tr key={endpoint}><td>{endpoint}</td><td>{detail}</td></tr>)}</tbody></table></div><h2>Seven days to download</h2><p>The clock starts after the upload completes. New downloads are blocked after seven days. Anyone with the link can download before then. The sender can revoke it earlier.</p><p>In this preview, expired storage is cleared when another transfer is created or an owner runs cleanup. Automatic background cleanup, payment signals, and outbound webhooks are next steps. Unfinished uploads expire after 24 hours.</p><a className="doc-file" href="/llms.txt"><Terminal size={18}/>Read the complete agent instructions <ArrowUpRight size={16}/></a></article><footer><span>bilaga / File delivery for agents.</span><a href="/">Back to Bilaga <ArrowUpRight size={14}/></a></footer></main>;}
